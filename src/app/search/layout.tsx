import type { Metadata } from "next";

// Сама анкета — клиентский компонент, а метаданные из такого экспортировать
// нельзя. Поэтому заголовок и описание страницы живут в этом слое: без них
// в выдаче у анкеты стоял бы заголовок главной.
export const metadata: Metadata = {
  title: "Подобрать подарок",
  description:
    "Заполните короткую анкету — кому дарим, по какому поводу, какой бюджет " +
    "и сколько есть времени. Дарибот предложит идеи с объяснением, почему " +
    "они подойдут. Первый подбор — без регистрации.",
  alternates: { canonical: "/search" },
};

export default function SearchLayout({ children }: LayoutProps<"/search">) {
  return children;
}
